export interface Product {
    title: string;
    imageUrl: string;
    id: number;
    description: string;
    amount: number;
    createdAt: string;
    updatedAt: string;
    isAvailable: boolean;
}
